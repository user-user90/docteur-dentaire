import heroSection from "./heroSection"
import reservation from "./reservation"
import services from "./services"
import dentaire from "./dentaire"
export const schema = {
  types: [heroSection,reservation,services,dentaire],
}
